let message: string = "Chào bạn! Đây là TypeScript.";
let year: number = 2024;

function sayHello(name: string): string {
    return `Hello ${name}, chào mừng bạn đến với năm ${year}!`;
}

console.log(sayHello("Học viên"));